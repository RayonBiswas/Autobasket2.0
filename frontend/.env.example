# Copy to .env.local (gitignored) and adjust.
# Backend API base URL
VITE_API_URL=http://127.0.0.1:8000
# Extra hostnames allowed to reach the Vite dev server, comma-separated (e.g. an ngrok domain)
# VITE_ALLOWED_HOSTS=
