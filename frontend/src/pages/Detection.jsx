function Detection() {
  return (
    <div className="page">
      <div className="page-head">
        <h1>Camera</h1>
        <p>The fridge camera will recognise what's on each shelf, so you rarely have to tell the app what you put in.</p>
      </div>
      <div className="card empty">
        <h2>Coming with the hardware</h2>
        <p>
          Once the camera is installed, each time the door closes the app will take a photo of the shelf, name the
          items it sees, and suggest which slot they belong to. Until then, assign products on the Shelves page.
        </p>
      </div>
    </div>
  );
}

export default Detection;
